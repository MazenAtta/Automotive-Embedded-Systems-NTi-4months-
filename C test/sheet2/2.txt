/********************************************************************************************************************************************************\
#include "stdio.h"
#include "stdlib.h"

void main()
{
   
    double r,Area, Circumference;
    printf("please enter the radius\n");
    scanf("%lf", &r);
    Area = (3.14) * r * r;
    Circumference = 2 * (3.14) * r;
    printf("the area euqals = %.2f\n", Area);
    printf("the area euqals = %.2f\n", Circumference);
}
/********************************************************************************************************************************************************\
#include "stdio.h"
#include "stdlib.h"

void main()
{

    int n;
    char c;

    printf("please enter a number\n");
    scanf("%d", &n);
    fflush(stdin);
    printf("please enter a character\n");
    scanf("%c", &c);
    if ((n & 1) == 0)
    {
        printf("%c\n", c);
    }
    else if ((n & 1) == 1)

    {
        if ((c - 'a') > 0)
        {
            c = c - 'a' + 'A';
            printf("%c", c);
        }
        else if ((c - 'a') < 0)
        {
            c = c - 'A' + 'a';

            printf("%c", c);
        }
    }
}
/********************************************************************************************************************************************************\
#include "stdio.h"
#include "stdlib.h"

void main()
{

    int n1 = 0, n2 = 0, rem = 0, c = 0;
    printf("please enter a number\n");
    scanf("%d", &n1);
    while (n1)
    {
        rem = n1 % 10;
        n1= n1 / 10;
        n2 = n2 * 10 + rem;
    }
    printf("%d",n2);
}
/********************************************************************************************************************************************************\
#include "stdio.h"
#include "stdlib.h"

void main()
{

    unsigned int n1 = 0, i = 0, rem = 0, c = 0;
    printf("please enter a number\n");
    scanf("%d", &n1);
    while (n1)
    {
        if ((n1 & 1) == 1)
        {
            c++;
        }
        n1 = (n1 >> 1);
    }
    printf("the number of 1's in the number = %d", c);
}
/********************************************************************************************************************************************************\
#include "stdio.h"
#include "stdlib.h"

void main()
{

    int n1 = 0, i, rem = 0, c = 0;
    printf("please enter a number\n");
    scanf("%d", &n1);
    for (i = 31; i >= 0; i--)
    {
        if ((n1 >> i) & 1)
        {
            printf("%d", 1);
            c = 1;
        }
        else if (c == 1)
        {
            printf("%d", 0);
        }
    }
}

#include "stdio.h"
#include "stdlib.h"

void main()
{

    unsigned int n1 = 0, i = 0, n2 = 0, c = 0;
    printf("please enter a number\n");
    scanf("%d", &n1);
    n2 = 1 << 31;
    while ((n1 & n2) == 0)
    {
        n2 = (n2 >> 1);
    }

    while (n2)
    {

        if ((n1 & n2) >= 1)
        {
            printf("%d", 1);
        }
        else
        {
            printf("%d", 0);
        }
        n2 = (n2 >> 1);
    }
}
/********************************************************************************************************************************************************\
#include "stdio.h"
#include "stdlib.h"

void main()
{

    int n1 = 0, i = 0, rem = 0, c = 0;
    printf("please enter a number\n");
    scanf("%d", &n1);
    while (n1)
    {
        printf("%d", n1 & 1);
        n1 = (n1 >> 1);
        i++;
    }
if(i != 32){
    for(i;i<32;i++){
        printf("0");
    }
}
}

#include "stdio.h"
#include "stdlib.h"

void main()
{

    int n1 = 0, i = 0, rem = 0, c = 0;
    printf("please enter a number\n");
    scanf("%d", &n1);
    for (i = 31; i >= 0; i--)
    {
        printf("%d", n1 & 1);
        n1 = (n1 >> 1);
    }
}

#include "stdio.h"
#include "stdlib.h"


void main()
{

    int n1 = 0, n2 = 0, i, j = 31, rem = 0, c = 0;
    printf("please enter a number\n");
    scanf("%d", &n1);
    for (i = 0; i <= 31; i++)
    {
        if (n1 >> i & 1)
        {
            n2 = n2 | (1 << j);
        }
        j--;
    }
    print_binary(n2);
}
/********************************************************************************************************************************************************\
#include "stdio.h"
#include "stdlib.h"

void main()
{
    unsigned int a, b, n, sum;
    printf("please enter the first number\n");
    scanf("%d", &a);
    printf("please enter the second number\n");
    scanf("%d", &b);
    n = b - a + 1;
    sum = (n) * (b+a) / 2;
    printf("the sum of numbers from %d to %d equals %d", a, b, sum);
}
/********************************************************************************************************************************************************\
#include "stdio.h"
#include "stdlib.h"

void main()
{

    unsigned int Set_bit, Clear_bit, Toggle_bit, Read_bit, Bit_num, x, n1, n2;
    n2 = 1 << 31;

    printf("please choose the operation you want \n1-Set_bit\n2-Clear_bit\n3-Toggle_bit\n4-Read_bit\n");
    scanf("%d", &x);
    printf("please enter the number\n");
    scanf("%d", &n1);
    if (x != 4)
    {
        printf("please enter the bit number\n");
        scanf("%d", &Bit_num);
    }
    switch (x)
    {
    case 1:
        n1 = (1 << Bit_num) | n1;
        break;
    case 2:
        n1 = (~(1 << Bit_num)) & n1;
        break;
    case 3:
        n1 = (1 << Bit_num) ^ n1;
        break;
    case 4:
        n2 = (n1 >> Bit_num) & 1;
        printf("The bit value = %d\n ",n2);
        break;
    default:
        break;
    }
    if (x == 1 || x == 2 || x == 3)
    {
        printf("the number in decimal is %d \n", n1);

        printf("the number in binary is  ");
        print_binary(n1);
        
    }
}
/********************************************************************************************************************************************************\
